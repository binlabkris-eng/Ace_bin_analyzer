# Apple BIN Device Structure Analyzer (MVP)

Local web app for analyzing Apple `.bin` dumps in the browser (no uploads).

## Run

```bash
cd apple-bin-analyzer
npm install
npm run dev
```

## Tests

```bash
npm test
```

